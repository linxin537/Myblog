● 前端启动命令：

  cd C:\Mywebsite_2\frontend
  npm run dev


● 后端启动命令：
python -m uvicorn app.main:app --host 127.0.0.1 --port 8003


deepseek
sk-93d4658c77bd4b22a51d62c2fd996e82


 UPDATE users SET role = 'admin' WHERE username = 'linxin';





● Phase 5 部署已完成。创建了以下 8 个文件：

  ┌─────────────────────┬────────────────────────────────────────────────┐
  │        文件         │                      用途                      │
  ├─────────────────────┼────────────────────────────────────────────────┤
  │ .env.production     │ 生产环境变量模板，复制为 .env 后修改           │
  ├─────────────────────┼────────────────────────────────────────────────┤
  │ Dockerfile          │ 后端 Python 3.12 + FastAPI 镜像                │
  ├─────────────────────┼────────────────────────────────────────────────┤
  │ Dockerfile.frontend │ 前端多阶段构建（Node 编译 → Nginx 服务）       │
  ├─────────────────────┼────────────────────────────────────────────────┤
  │ nginx.conf          │ 反向代理 + SPA 路由 + gzip + 安全头            │
  ├─────────────────────┼────────────────────────────────────────────────┤
  │ docker-compose.yml  │ MySQL + FastAPI + Nginx 三服务编排，带健康检查 │
  ├─────────────────────┼────────────────────────────────────────────────┤
  │ scripts/backup.sh   │ Linux 数据库 + 文件备份，支持 crontab 自动执行 │
  ├─────────────────────┼────────────────────────────────────────────────┤
  │ scripts/backup.ps1  │ Windows PowerShell 版本备份脚本                │
  ├─────────────────────┼────────────────────────────────────────────────┤
  │ DEPLOY.md           │ 完整部署文档（部署、HTTPS、运维、故障排查）    │
  └─────────────────────┴────────────────────────────────────────────────┘

  部署到云服务器只需 4 步

  git clone <repo> /opt/blog && cd /opt/blog
  cp .env.production .env && nano .env   # 改密码
  docker compose up -d                    # 一键启动
  curl http://localhost/api/v1/health     # 验证

  架构图

  浏览器 → :80 → Nginx ─┬─ /api/*    → FastAPI :8000
                        ├─ /static/* → FastAPI :8000
                        └─ /*        → 前端静态文件 (SPA)
                     MySQL :3306 (内网)

  项目所有 5 个开发阶段全部完成。
